PACKAGE cogroo.ruta;

TYPESYSTEM TypeSystem;

IMPORT opennlp.uima.Token FROM TypeSystem AS cgToken;

DECLARE Annotation PROBLEM (String id, String segundo);
cgToken cgToken cgToken {-> CREATE(PROBLEM, "id" = "1", "segundo" = "2")};

//DECLARE Annotation PROBLEM (String id);
//cgToken{REGEXP(cgToken.lemma, "([^,]*,)*(o)(,[^,]*)*", true) -> CREATE(PROBLEM, "id" = "1")};

